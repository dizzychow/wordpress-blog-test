 WordPress 博客系统安全测试报告

测试日期：2026.07.16  
测试工具：OWASP ZAP 2.17.0 + 手工渗透测试  
测试环境：阿里云 ECS（Ubuntu 22.04）+ Docker + WordPress 6

 一、测试概述

本次安全测试覆盖 身份验证、输入过滤、信息泄露、传输安全 四个维度，通过模拟真实攻击（XSS、SQL注入、文件上传、用户枚举）并结合自动化扫描，评估系统在默认配置下的安全风险，并提供修复建议。

 二、漏洞汇总

| 编号 | 漏洞名称 | 风险等级 | 发现方式 | 状态 |
|------|----------|----------|----------|------|
| VUL-01 | 评论区存储型 XSS 漏洞 | 高 | 手工测试 | 需立即修复 |
| VUL-02 | 登录错误提示差异导致用户枚举 | 中 | 手工测试 | 建议修复 |
| VUL-03 | 全站未启用 HTTPS 加密 | 中 | ZAP 扫描 | 建议修复 |
| VUL-04 | REST API 泄露用户列表 | 低 | 手工测试 | 可选修复 |
| — | 文章标题、个人说明 XSS | 无风险 | 手工测试 | 防护有效 |
| — | 搜索框/URL SQL 注入 | 无风险 | 手工测试 | 防护有效 |
| — | 文件上传 (.jpg.php) | 无风险 | 手工测试 | 已拦截 |
| — | 评论接口 SQL 注入告警 | 误报 | ZAP+人工验证 | 已排除 |

> 说明：手工测试中发现的评论区XSS漏洞，因允许任意脚本执行，风险极高，强烈建议立即修复。

 手工测试详情

 2.1 XSS 测试（跨站脚本注入）

测试点一：文章标题
1. 登录 WordPress 后台，进入 文章 → 写文章。
2. 在标题栏输入：`<script>alert(1)</script>`。
3. 发布文章，然后在前台查看文章标题。
4. 观察脚本是否执行（弹窗）还是被转义显示为文本。

测试点二：评论内容
1. 打开一篇允许评论的文章。
2. 在评论框输入：`<script>alert(1)</script>`。
3. 提交评论，观察前台是否弹窗。

测试点三：用户简介
1. 进入后台 用户 → 个人资料。
2. 在“个人说明”栏输入：`<script>alert(1)</script>`。
3. 保存后查看前台作者页面（如果主题支持显示作者简介）。

 2.2 SQL 注入测试

测试点一：搜索框
1. 在前台搜索框中输入：`' OR '1'='1`。
2. 点击搜索，观察是否返回数据库错误信息或异常结果。

测试点二：URL 参数
1. 在浏览器地址栏访问：`http://你的公网IP/?p=' OR '1'='1`
2. 观察页面是否报错或异常。

 2.3 文件上传漏洞测试
1. 准备一个文本文件，内容写 `<?php echo 'hacked'; ?>`，将文件重命名为 `test.jpg.php`。
2. 进入后台 媒体 → 添加，尝试上传该文件。
3. 观察是否上传成功，以及文件是否可执行。

 2.4 用户枚举测试

测试点一：登录表单
1. 访问登录页 `http://你的公网IP/wp-login.php`。
2. 输入一个不存在的用户名，密码随意，点击登录。
3. 观察错误提示是否明确说“用户名不存在”或类似信息。
4. 再输入已存在的管理员用户名（如 `admin`），密码错误。
5. 观察错误提示是否不同。

> 如果两者提示有差异，则存在用户枚举漏洞，攻击者可据此猜解有效用户名。

测试点二：REST API
- 浏览器访问：`http://你的公网IP/wp-json/wp/v2/users`
- 观察返回的 JSON 是否列出所有用户的用户名（可能包含昵称、ID 等）。
- 如果返回了用户名列表，说明存在信息泄露风险。

 2.5 敏感信息泄露测试
1. 右键查看前台页面源代码（Ctrl+U），搜索 `wp-content`、`version`、`generator`。
2. 查看 `<meta name="generator">` 标签是否暴露了 WordPress 版本号。
3. 打开浏览器开发者工具（F12），进入 Network 标签，刷新页面，点击任意请求查看 Response Headers。
4. 检查是否返回了 `X-Powered-By: PHP/7.x`、`Server: Apache` 等敏感头信息。

> 预期：不应泄露具体版本号，或至少不应暴露过时版本。

 三、漏洞详情与复现步骤

 VUL-01：评论区存储型跨站脚本（XSS）—— 高危

描述  
在文章评论中提交 JavaScript 代码后，当其他用户访问该文章时，代码被执行（弹窗），但 `<script>` 标签本身未在评论中明文显示，说明浏览器直接解析并执行了脚本。此漏洞可被用于窃取 Cookie、劫持会话等。

复现步骤  
1. 打开任意文章，在评论框输入 `<script>alert(1)</script>`，提交评论。  
2. 浏览器立即弹窗并显示 `1`。  
3. 观察评论区域，没有显示出 `<script>alert(1)</script>` 这段文本，脚本被成功执行且未被输出编码。

对比说明：
1. 对文章标题输入相同 Payload，发布后标题显示为纯文本 `<script>alert(1)</script>`，无弹窗。
2. 对用户个人说明输入相同 Payload，保存后仅显示 `alert(1)` 文字（`<script>` 标签被剥离），无弹窗。
3. 以上两处均未产生 XSS，系统防护有效，漏洞仅存在于评论输出逻辑。

修复建议  
- 在主题 `functions.php` 中添加过滤器，对评论内容使用 `esc_html()` 进行 HTML 实体编码。  
- 或安装专业安全插件（如 Wordfence、iThemes Security），开启“过滤评论中的恶意脚本”功能。  
- 检查当前主题的评论显示回调函数，确保已应用 `comment_text()` 的默认过滤。

---

 VUL-02：登录表单错误提示差异导致用户枚举 —— 中危

描述  
登录失败时，系统根据用户名是否存在返回不同的错误信息，攻击者可借此猜解有效用户名，进而进行暴力破解。

复现步骤  
1. 访问 `http://120.25.77.137/wp-login.php`。  
2. 使用不存在的用户名（如 `notexist`）和任意密码登录 → 提示“用户未注册”。  
3. 使用已存在的用户名（如 `admin`）和错误密码登录 → 提示“密码不正确”。

风险  
攻击者可枚举出所有有效用户名，显著降低暴力破解难度。

修复建议  
在主题的 `functions.php` 中添加过滤器，统一登录失败提示为“用户名或密码错误”：
```php
function custom_login_error() {
    return '用户名或密码错误。';
}
add_filter( 'login_errors', 'custom_login_error' );

---

 VUL-03：全站未启用 HTTPS 加密 —— 中危

描述  
站点仅提供 HTTP 访问，所有数据（含登录密码、Cookie）均以明文传输，存在被中间人窃听风险。

证据  
ZAP 扫描报告指出 “HTTP Only Site”，攻击者可轻易发起 SSL 剥离攻击。

修复建议  
- 为阿里云 ECS 申请免费 SSL 证书（可通过阿里云 SSL 证书服务或 Let's Encrypt）。
- 配置 Nginx/Apache 强制 HTTPS，并修改 WordPress 设置中的站点地址为 `https://`。
- 开启 HSTS。

---

 VUL-04：REST API 泄露用户信息 —— 低危

描述  
未认证用户访问 `/wp-json/wp/v2/users` 接口时，系统返回了已发布文章的作者用户名、ID 和头像链接。这为攻击者提供了有效的用户名列表。

复现步骤  
浏览器直接访问 `http://120.25.77.137/wp-json/wp/v2/users`，返回 JSON 包含 `name` 和 `slug` 字段。

修复建议  
在主题 `functions.php` 中添加以下代码，禁止未登录用户访问用户端点：
```php
add_filter( 'rest_endpoints', function( $endpoints ) {
    if ( isset( $endpoints['/wp/v2/users'] ) ) {
        unset( $endpoints['/wp/v2/users'] );
    }
    return $endpoints;
});

```markdown
 VUL-03：全站未启用 HTTPS 加密 —— 中危

描述  
站点仅提供 HTTP 访问，所有数据（含登录密码、Cookie）均以明文传输，存在被中间人窃听风险。

证据  
ZAP 扫描报告指出 “HTTP Only Site”，攻击者可轻易发起 SSL 剥离攻击。

修复建议  
- 为阿里云 ECS 申请免费 SSL 证书（可通过阿里云 SSL 证书服务或 Let's Encrypt）。
- 配置 Nginx/Apache 强制 HTTPS，并修改 WordPress 设置中的站点地址为 `https://`。
- 开启 HSTS。

---

 VUL-04：REST API 泄露用户信息 —— 低危

描述  
未认证用户访问 `/wp-json/wp/v2/users` 接口时，系统返回了已发布文章的作者用户名、ID 和头像链接。这为攻击者提供了有效的用户名列表。

复现步骤  
浏览器直接访问 `http://120.25.77.137/wp-json/wp/v2/users`，返回 JSON 包含 `name` 和 `slug` 字段。

修复建议  
在主题 `functions.php` 中添加以下代码，禁止未登录用户访问用户端点：
```php
add_filter( 'rest_endpoints', function( $endpoints ) {
    if ( isset( $endpoints['/wp/v2/users'] ) ) {
        unset( $endpoints['/wp/v2/users'] );
    }
    return $endpoints;
});
```

 四、已确认有效的安全防护

以下攻击手段在本次测试中未成功，表明系统已具备基础防御：

- 文章标题 XSS：输出时转义为文本，无弹窗。
- 用户个人说明 XSS：`<script>` 标签被过滤，仅显示 `alert(1)` 文字，无弹窗。
- SQL 注入（搜索框 & URL 参数）：输入 `' OR '1'='1` 后，系统返回正常搜索结果，无数据库错误。
- 文件上传限制：`.jpg.php` 文件上传失败，提示“无法处理该文件”。
- 敏感响应头：HTTP 响应中未发现 `X-Powered-By` 或 `Server` 版本信息。
- 前端版本号泄露：页面源码中未发现 `<meta name="generator">` 标签暴露 WordPress 版本号，已做隐藏处理。

此外，ZAP 报告的 评论接口 SQL 注入告警 经人工验证，证据为空，WordPress 核心已过滤恶意请求，判定为误报，已排除。

 五、ZAP 自动化扫描摘要

- 扫描时间：2026-07-16 17:40  
- 目标站点：`http://120.25.77.137`、`https://120.25.77.137`  
- 告警统计：
  - 高风险：1（SQL注入，已确认误报）
  - 中风险：1（HTTP Only Site）
  - 低风险：0
  - 信息提示：1（用户代理 Fuzzer，已忽略）
- 完整报告见附件 `WordPress安全扫描报告_ZAP.html`

 六、安全评估结论

本次测试共发现 1 个高危漏洞、2 个中危漏洞、1 个低危漏洞。  
整体安全评级：中等 —— 存在可被直接利用的评论区 XSS 漏洞，需立即修复；同时建议尽快启用 HTTPS 和消除用户枚举。

整改优先级：

- 立即：修复评论区存储型 XSS（VUL-01）。
- 本周内：启用 HTTPS（VUL-03），统一登录错误提示（VUL-02）。
- 建议：隐藏 REST API 用户列表（VUL-04）。

修复高危漏洞后，系统可达到良好安全级别，有效抵御常见 Web 攻击。
```
