# WordPress 功能性测试总结报告

## 1. 测试概况

- **测试版本**：WordPress 6，运行于阿里云 ECS
- **测试周期**：2026-07-09 至 2026-07-11
- **测试人员**：周芷羽
- **用例总数**：48 条

**执行情况**：
- 通过：44 条
- 失败：4 条
- 阻塞：0 条

## 2. 缺陷统计

- **发现缺陷总数**：4 个

**按严重程度分布**：

| 严重程度 | 数量 |
|----------|------|
| 致命 | 0 |
| 严重 | 2 |
| 一般 | 2 |
| 建议 | 0 |

**按模块分布**：

| 模块 | 缺陷数 |
|------|--------|
| 权限控制 | 1 |
| 文章管理 | 1 |
| 评论 | 2 |
| 媒体库 | 0 |
| 搜索 | 0 |
| 异常/安全 | 0 |

## 3. 典型缺陷分析

### 3.1 严重缺陷

#### WF-005：编辑B尝试删除他人文章

- **现象**：编辑B在“所有文章”中勾选管理员发布的文章并执行“移到回收站”操作，文章被成功移至回收站，未提示权限不足。
- **风险**：低权限用户可删除管理员内容，破坏数据完整性，违反角色权限设计。
- **建议**：增加操作前权限校验，仅允许作者本人及更高权限角色（如管理员、编辑）删除，编辑角色应限制对他人文章的删除/移动权限。

#### WF-044：XSS评论注入（未转义）

- **现象**：提交评论内容 `<img src=x onerror=alert(1)>` 后，页面未触发弹窗，但源码显示标签未被转义，DOM 中渲染为 HTML 元素，最终显示为裂图（图片加载失败）。
- **风险**：尽管 `alert` 未触发（可能被浏览器或 CSP 拦截），但标签未被转义意味着存在潜在的 XSS 漏洞风险，攻击者可能通过其他事件或属性执行恶意脚本。
- **建议**：对所有用户输入的评论内容进行严格的 HTML 转义（如使用 `esc_html` 或 `wp_kses`），确保特殊字符以纯文本显示，杜绝任何 HTML 解析。

### 3.2 一般缺陷

#### WF-010：草稿状态异常可见

- **现象**：管理员保存草稿后，前台首页竟可见该文章，但点击进入无法编辑（仅显示“不可编辑”状态）。预期草稿应完全不可见。
- **影响**：草稿内容泄露给访客，且提供误导性入口。
- **建议**：检查文章状态查询条件，确保 `post_status='draft'` 的文章不进入前台循环。

#### WF-026：超长评论未拦截

- **现象**：提交 5000 字符的评论，系统未截断、未报错，直接在前台完整显示该评论。
- **影响**：可能导致页面布局错乱或数据库存储压力，不符合输入约束预期。
- **建议**：在前端和后端双重增加字符长度校验，给出明确提示并拒绝保存。

## 4. 测试结论与建议

本次功能测试覆盖了 WordPress 核心前台与后台模块，整体功能基本可用，但存在 **2 个严重权限与安全漏洞**（越权删除、XSS注入风险）及 **2 个一般功能缺陷**（草稿可见、超长评论未处理）。

### 上线建议

- **条件放行**，但必须在上线前修复 WF-005 和 WF-044 两个严重问题，并复测验证通过。
- 一般缺陷可排期在下个迭代优化，不影响基础业务流转。
- 后续应加强输入校验（长度、XSS过滤）及细粒度权限验证的自动化回归测试。

### 功能测试结论

> **条件放行**，需修复严重缺陷后复测。
